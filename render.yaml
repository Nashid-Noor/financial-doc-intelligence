services:
  - type: web
    name: financial-doc-intelligence
    env: docker
    plan: free
    buildCommand: docker build -t financial-doc-app .
    startCommand: ./start.sh
    envVars:
      - key: HF_API_KEY
        sync: false
      - key: API_URL
        value: http://localhost:8000
